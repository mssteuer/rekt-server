import { CLValue } from 'casper-js-sdk';
import { WithRemainder } from './casper/types';
import { Schema, Schemas } from './schema';
export interface Event {
    name: string;
    contractHash: Uint8Array | null;
    contractPackageHash: Uint8Array | null;
    data: Record<string, CLValue>;
}
export declare function parseEventNameWithRemainder(rawEvent: Uint8Array): WithRemainder<string>;
export declare function parseEventNameAndData(rawEvent: string, schemas: Schemas): {
    name: string;
    data: Record<string, CLValue>;
};
export declare function parseEventDataFromBytes(schema: Schema, rawBytes: Uint8Array): Record<string, CLValue>;
